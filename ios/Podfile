# Uncomment this line to define a global platform for your project
platform :ios, '15.0'

# CocoaPods analytics sends network requests when determining platform versions
# This can lead to errors in some network environments.
# Uncomment the following line to disable CocoaPods analytics.
# ENV['COCOAPODS_DISABLE_STATS'] = 'true'

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  # List Flutter dependencies manually to avoid compatibility issues
  # Note: This is a simplified setup. Some plugins might require additional configuration.
  pod 'Firebase/Core', '~> 10.0.0'  # Example placeholder

  # Flutter plugins from pubspec.yaml
  # Uncomment and configure as needed:
  # pod 'flutter_vpn'
  # pod 'permission_handler'
  # pod 'flutter_secure_storage'
  # pod 'path_provider'
  # pod 'shared_preferences'
  # pod 'provider'
end
