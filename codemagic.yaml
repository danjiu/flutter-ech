workflows:
  ios-build:
    name: iOS Simulator Build Only
    environment:
      vars:
        FLUTTER_VERSION: 3.38.5
        XCODE_VERSION: 26.1
        # 强制设置为模拟器构建
        FLUTTER_TARGET_PLATFORM: ios-simulator
        CODESIGNING_ALLOWED: NO
      xcode: latest
      cocoapods: default
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
          source: true
    scripts:
      - name: Clean environment
        script: |
          # 安全清理环境，忽略不存在的文件
          rm -rf build/ || true
          rm -rf ios/.symlinks/ || true
          rm -rf ios/Pods/ || true
          rm -f ios/Podfile.lock || true
          echo "✅ 环境清理完成"

      - name: Set up Flutter
        script: |
          echo "⚙️ 设置Flutter 3.38.5..."

          # 检查当前Flutter版本
          echo "当前Flutter版本:"
          flutter --version

          # 如果需要，切换到正确的Flutter版本
          if ! flutter --version | grep -q "3.38.5"; then
            echo "切换到Flutter 3.38.5..."
            flutter channel stable
            flutter upgrade 3.38.5
          fi

          # 再次确认版本
          echo "最终Flutter版本:"
          flutter --version

          # 验证Dart SDK版本
          echo "Dart版本:"
          dart --version

          # Flutter doctor
          echo "Flutter doctor检查:"
          flutter doctor -v

          # 预热Flutter
          echo "预加热Flutter..."
          flutter precache --ios
          echo "✅ Flutter设置完成"

      - name: Install dependencies
        script: |
          echo "📦 安装Flutter依赖..."

          # 再次确认Flutter版本
          flutter --version

          # 清理缓存
          flutter clean

          # 获取依赖
          echo "运行 flutter pub get..."
          flutter pub get

          # 检查是否成功
          if [ $? -eq 0 ]; then
            echo "✅ Flutter依赖安装成功"
          else
            echo "❌ Flutter依赖安装失败，尝试解决方案..."
            # 尝试升级依赖
            flutter pub upgrade
            # 再次尝试获取
            flutter pub get
          fi

          echo "依赖验证:"
          flutter pub deps

      - name: Verify iOS Podfile
        script: |
          cd ios
          echo "📋 现有Podfile内容:"
          echo "====================="
          cat Podfile
          echo "====================="
          echo "✅ Podfile验证完成"

      - name: Install Pods
        script: |
          cd ios

          echo "📲 安装iOS Pods..."

          # 首先更新CocoaPods仓库
          echo "更新CocoaPods仓库..."
          pod repo update

          # 清理Pod缓存
          echo "清理Pod缓存..."
          pod cache clean --all

          # 安装Pods
          echo "运行 pod install..."
          pod install --repo-update

          # 检查安装结果
          if [ $? -eq 0 ]; then
            echo "✅ Pods安装成功"
            echo "生成的文件:"
            ls -la *.xcworkspace || true
            ls -la Pods/ || true
          else
            echo "❌ Pods安装失败，尝试删除Podfile.lock重试..."
            rm -f Podfile.lock
            pod install
          fi

      - name: Force Simulator Build
        script: |
          # 设置环境变量强制模拟器构建
          export FLUTTER_TARGET_PLATFORM=ios-simulator
          export CODESIGNING_ALLOWED=NO

          # 构建模拟器版本，明确指定设备
          flutter clean
          flutter pub get

          # 使用xcodebuild直接构建模拟器版本
          xcodebuild -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' \
            ONLY_ACTIVE_ARCH=NO \
            CODE_SIGNING_ALLOWED=NO \
            ENABLE_BITCODE=NO \
            build

          # 如果xcodebuild失败，尝试Flutter方法
          if [ $? -ne 0 ]; then
            echo "Flutter fallback method..."
            flutter build ios --simulator --release --no-codesign
          fi

      - name: Verify build
        script: |
          # 验证构建产物
          ls -la build/ios/iphonesimulator/ || ls -la build/ios/
          find . -name "*.app" -type d

    artifacts:
      - build/ios/**/Runner.app
      - build/ios/Build/Products/Release-iphonesimulator/*.app
